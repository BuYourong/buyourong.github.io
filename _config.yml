title: "与骐骥亢轭的博客"
name: "与骐骥亢轭"
description: "与骐骥亢轭的个人技术博客"

avatar: ../images/avatar.jpg
favicon: ../images/favicon.ico

navigation:
  - name: 首页
    url: /
  - name: 关于
    url: /about
  - name: 分类
    url: /tags

paginate: 8

excerpt_separator: <!-- more -->

footer-links:
  zhihu: 
  email: contact@qjke.top
  facebook:
  github: 
  rss: # just type anything here for a working RSS icon
  twitter: 
  youtube:

# footer-text: Copyright (c) 2026 与骐骥亢轭

# 添加起始年份配置
start_year: 2026

url: https://buyourong.top

permalink: /:title/
paginate_path: /page:num/

# Gitalk 用于给博客文章引入评论功能，配置方法请参考 https://github.com/gitalk/gitalk?tab=readme-ov-file#usage
# gitalk:
#   clientID: 
#   clientSecret: 
#   repo: buyourong.github.io
#   owner: buyourong

# The release of Jekyll Now
version: v1.2.0

# Jekyll 3 now only supports Kramdown for Markdown
kramdown:
  # Use GitHub flavored markdown, including triple backtick fenced code blocks
  input: GFM
  # Jekyll 3 and GitHub Pages now only support rouge for syntax highlighting
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    # Use existing pygments syntax highlighting css
    css_class: 'highlight'

# Set the Sass partials directory, as we're using @imports
sass:
  style: expanded # You might prefer to minify using :compressed

# Use the following plug-ins
plugins:
  - jekyll-sitemap # Create a sitemap using the official Jekyll sitemap gem
  - jekyll-feed # Create an Atom feed using the official Jekyll feed gem
  - jekyll-paginate
  - jekyll-mermaid

# Exclude these files from your production _site
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - .well-known/

# Mermaid 配置
mermaid:
  # 版本，可选：latest 或指定版本
  version: "latest"
  # 主题：default, dark, forest, neutral
  theme: "default"
  # 配置选项
  config:
    startOnLoad: true
    securityLevel: 'loose'
    themeCSS: |
      .mermaid { font-family: 'Arial'; }